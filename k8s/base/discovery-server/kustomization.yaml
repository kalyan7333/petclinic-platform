apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Base manifests for discovery-server (PETPLAT-40).
# Namespace, replica count and image tag are set by the environment overlays
# in k8s/overlays/dev/.
resources:
  - serviceaccount.yaml
  - configmap.yaml
  - service.yaml
  - deployment.yaml
